# ✅ Чеклист перед развертыванием

Используйте этот чеклист перед каждым развертыванием на production.

## 📋 Обязательные проверки

### 1. Код и конфигурация
- [ ] Все изменения закоммичены в Git
- [ ] Нет console.log или отладочного кода
- [ ] `.env` файл НЕ добавлен в Git
- [ ] `.env.example` обновлен (если добавлены новые переменные)
- [ ] `package.json` содержит все необходимые зависимости

### 2. Автоматическая проверка готовности
```bash
npm run health-check
```
- [ ] Health check проходит успешно
- [ ] Нет критических ошибок
- [ ] Все предупреждения рассмотрены

### 3. Локальное тестирование
```bash
npm start
```
- [ ] Сервер запускается без ошибок
- [ ] API endpoints работают
- [ ] Загрузка изображений работает
- [ ] Данные сохраняются корректно

### 4. Переменные окружения
- [ ] `PORT` настроен корректно
- [ ] `NODE_ENV=production` для production
- [ ] `DB_FILE` указан (если используется)
- [ ] `MAX_FILE_SIZE` установлен разумно
- [ ] CORS настроен для вашего домена (если нужно)

### 5. База данных
- [ ] ⚠️ JSON файл НЕ используется в production (или понимаете ограничения)
- [ ] Если используете настоящую БД: строка подключения настроена
- [ ] Миграции выполнены (если применимо)
- [ ] Резервная копия создана

### 6. Файлы и хранилище
- [ ] ⚠️ Понимаете, что локальные uploads могут быть удалены (Heroku, Railway)
- [ ] Настроено облачное хранилище (S3, Cloudinary) для production
- [ ] Или используете платформу с persistent storage

### 7. Безопасность
- [ ] HTTPS настроен (большинство платформ предоставляют автоматически)
- [ ] CORS настроен для вашего домена
- [ ] Rate limiting добавлен (опционально, но рекомендуется)
- [ ] Helmet.js добавлен для безопасности заголовков (опционально)
- [ ] Секретные ключи не в коде, только в .env

### 8. Производительность
- [ ] Лимиты размера файлов установлены разумно
- [ ] Компрессия изображений настроена (опционально)
- [ ] Кэширование статических файлов настроено

### 9. Мониторинг и логирование
- [ ] Логирование настроено
- [ ] Мониторинг ошибок настроен (опционально)
- [ ] Знаете как проверить логи на вашей платформе

## 🚀 Готово к развертыванию!

Если все пункты отмечены, вы готовы к развертыванию!

### Быстрые команды

**Проверка готовности:**
```bash
npm run health-check
```

**Тест локально:**
```bash
npm start
# Откройте http://localhost:3000
```

**Деплой на различные платформы:**

**Railway:**
```bash
# Просто git push - Railway деплоит автоматически
git push origin main
```

**Heroku:**
```bash
git push heroku main
```

**Vercel:**
```bash
vercel --prod
```

**VPS с PM2:**
```bash
pm2 restart bajar
# или
pm2 start ecosystem.config.js --env production
```

## ⚠️ Важные напоминания

1. **JSON база данных**
   - ❌ НЕ рекомендуется для production
   - ✅ Используйте PostgreSQL, MongoDB, или MySQL

2. **Локальное хранилище файлов**
   - ❌ На Heroku/Railway файлы удаляются при рестарте
   - ✅ Используйте S3, Cloudinary, или подобное

3. **Переменные окружения**
   - ❌ Никогда не коммитьте .env в Git
   - ✅ Настройте их на платформе хостинга

4. **Резервное копирование**
   - ✅ Делайте регулярные бэкапы БД
   - ✅ Храните бэкапы в безопасном месте

## 🔄 После развертывания

- [ ] Проверьте что сайт открывается
- [ ] Протестируйте основные функции
- [ ] Проверьте логи на наличие ошибок
- [ ] Обновите `API_BASE_URL` в клиентских файлах
- [ ] Протестируйте добавление товара
- [ ] Протестируйте загрузку изображений

## 📞 Если что-то пошло не так

1. **Проверьте логи** на платформе хостинга
2. **Проверьте переменные окружения**
3. **Убедитесь что все зависимости установлены**
4. **Проверьте версию Node.js**
5. **Смотрите [DEPLOYMENT.md](DEPLOYMENT.md)** для решения проблем

---

**Последняя проверка:** `npm run health-check`

Удачного развертывания! 🚀
