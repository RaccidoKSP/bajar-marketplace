# 🚀 Руководство по развертыванию BAJAR

Это руководство поможет вам развернуть приложение BAJAR на различных хостинг-платформах.

## 📋 Предварительные требования

- Node.js версии 14.0.0 или выше
- npm версии 6.0.0 или выше
- Git (для развертывания через GitHub)

## 🔧 Подготовка проекта

### 1. Установка зависимостей

```bash
npm install
```

### 2. Настройка переменных окружения

Создайте файл `.env` в корневой папке проекта (используйте `.env.example` как шаблон):

```bash
cp .env.example .env
```

Отредактируйте `.env` файл под ваши нужды:

```env
PORT=3000
NODE_ENV=production
DB_FILE=database.json
UPLOAD_DIR=uploads
MAX_FILE_SIZE=10485760
MAX_FILES=5
```

### 3. Тестирование локально

```bash
# Запуск в режиме разработки
npm run dev

# Запуск в production режиме
npm start
```

## 🌐 Развертывание на различных платформах

### Heroku

1. **Установите Heroku CLI**
   ```bash
   # Скачайте с https://devcenter.heroku.com/articles/heroku-cli
   ```

2. **Войдите в Heroku**
   ```bash
   heroku login
   ```

3. **Создайте приложение**
   ```bash
   heroku create your-app-name
   ```

4. **Настройте переменные окружения**
   ```bash
   heroku config:set NODE_ENV=production
   heroku config:set MAX_FILE_SIZE=10485760
   ```

5. **Разверните приложение**
   ```bash
   git push heroku main
   ```

6. **Откройте приложение**
   ```bash
   heroku open
   ```

**Важно для Heroku:**
- Heroku использует эфемерную файловую систему - загруженные файлы будут удалены при рестарте dyno
- Рекомендуется использовать облачное хранилище (AWS S3, Cloudinary) для production
- Database.json будет сброшен при каждом деплое - используйте PostgreSQL или MongoDB addon

### Railway

1. **Создайте аккаунт на [Railway.app](https://railway.app/)**

2. **Создайте новый проект**
   - Подключите ваш GitHub репозиторий
   - Railway автоматически обнаружит Node.js проект

3. **Настройте переменные окружения** в разделе Variables:
   ```
   NODE_ENV=production
   PORT=3000
   ```

4. **Разверните**
   - Railway автоматически запустит `npm start`

### Render

1. **Создайте аккаунт на [Render.com](https://render.com/)**

2. **Создайте новый Web Service**
   - Подключите ваш GitHub репозиторий
   - Build Command: `npm install`
   - Start Command: `npm start`

3. **Настройте переменные окружения**:
   ```
   NODE_ENV=production
   ```

4. **Разверните**

### DigitalOcean App Platform

1. **Создайте аккаунт на [DigitalOcean](https://www.digitalocean.com/)**

2. **Создайте новое приложение**
   - Выберите ваш GitHub репозиторий
   - Выберите Node.js

3. **Настройте:**
   - Run Command: `npm start`
   - Environment Variables: добавьте необходимые переменные

4. **Разверните**

### VPS (Ubuntu/Debian)

1. **Подключитесь к вашему серверу**
   ```bash
   ssh user@your-server-ip
   ```

2. **Установите Node.js и npm**
   ```bash
   curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
   sudo apt-get install -y nodejs
   ```

3. **Установите PM2 (процесс-менеджер)**
   ```bash
   sudo npm install -g pm2
   ```

4. **Клонируйте репозиторий**
   ```bash
   git clone your-repository-url
   cd bajar-marketplace
   ```

5. **Установите зависимости**
   ```bash
   npm install --production
   ```

6. **Создайте .env файл**
   ```bash
   nano .env
   # Добавьте ваши переменные окружения
   ```

7. **Запустите приложение с PM2**
   ```bash
   pm2 start server.js --name bajar
   pm2 save
   pm2 startup
   ```

8. **Настройте Nginx как обратный прокси**
   ```bash
   sudo apt install nginx
   sudo nano /etc/nginx/sites-available/bajar
   ```

   Добавьте конфигурацию:
   ```nginx
   server {
       listen 80;
       server_name yourdomain.com;

       location / {
           proxy_pass http://localhost:3000;
           proxy_http_version 1.1;
           proxy_set_header Upgrade $http_upgrade;
           proxy_set_header Connection 'upgrade';
           proxy_set_header Host $host;
           proxy_cache_bypass $http_upgrade;
       }

       location /uploads {
           alias /path/to/your/project/uploads;
           expires 30d;
           add_header Cache-Control "public, immutable";
       }
   }
   ```

   Активируйте конфигурацию:
   ```bash
   sudo ln -s /etc/nginx/sites-available/bajar /etc/nginx/sites-enabled/
   sudo nginx -t
   sudo systemctl restart nginx
   ```

9. **Настройте SSL с Let's Encrypt (опционально)**
   ```bash
   sudo apt install certbot python3-certbot-nginx
   sudo certbot --nginx -d yourdomain.com
   ```

## 📁 Постоянное хранилище файлов

Для production рекомендуется использовать облачное хранилище:

### AWS S3
- Используйте `multer-s3` вместо локального хранилища
- Инструкции: https://www.npmjs.com/package/multer-s3

### Cloudinary
- Бесплатный план для небольших проектов
- Инструкции: https://cloudinary.com/documentation/node_integration

### DigitalOcean Spaces
- S3-совместимое хранилище
- Дешевле чем AWS S3

## 🗄️ База данных для Production

Для production использование JSON файла не рекомендуется. Рассмотрите:

### PostgreSQL
```bash
npm install pg
```

### MongoDB
```bash
npm install mongodb mongoose
```

### SQLite (для небольших проектов)
```bash
npm install better-sqlite3
```

## 🔒 Безопасность

1. **Всегда используйте HTTPS в production**
2. **Настройте CORS** для вашего домена:
   ```javascript
   app.use(cors({
     origin: 'https://yourdomain.com'
   }));
   ```
3. **Ограничьте размер запросов**
4. **Используйте rate limiting**:
   ```bash
   npm install express-rate-limit
   ```
5. **Добавьте helmet для безопасности заголовков**:
   ```bash
   npm install helmet
   ```

## 📊 Мониторинг

- **PM2 Monitoring**: `pm2 monit`
- **Logs**: `pm2 logs bajar`
- Используйте сервисы мониторинга: New Relic, DataDog, или встроенные инструменты вашего хостинга

## 🔄 Обновление приложения

### На VPS с PM2:
```bash
git pull
npm install
pm2 restart bajar
```

### На платформах (Heroku, Railway, Render):
- Просто сделайте push в ваш GitHub репозиторий
- Автоматический деплой настроен по умолчанию

## ❓ Проблемы и решения

### Порт уже используется
```bash
# Найдите процесс
lsof -i :3000
# Убейте процесс
kill -9 <PID>
```

### Недостаточно памяти
- Увеличьте размер dyno/instance
- Оптимизируйте загрузку изображений
- Используйте компрессию изображений

### Файлы не загружаются
- Проверьте права доступа к папке uploads
- Убедитесь что папка exists
- Проверьте настройки CORS

## 📞 Поддержка

Если возникли проблемы, проверьте:
1. Логи приложения
2. Переменные окружения
3. Версию Node.js
4. Права доступа к файлам

## 🎉 Готово!

Ваше приложение BAJAR готово к работе в production!
