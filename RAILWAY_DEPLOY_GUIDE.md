# 🚂 Пошаговое развертывание на Railway

## ⏱️ Время: 5 минут | Сложность: ⭐ Легко

---

## 📋 Что вам понадобится

- ✅ Аккаунт GitHub (для подключения репозитория)
- ✅ Этот проект загружен на GitHub
- ⏰ 5 минут свободного времени

---

## 🚀 Шаг за шагом

### Шаг 1: Подготовка GitHub репозитория

#### 1.1. Инициализируйте Git (если еще не сделано)

```bash
git init
```

#### 1.2. Добавьте файлы

```bash
git add .
```

#### 1.3. Сделайте коммит

```bash
git commit -m "Подготовка к развертыванию на Railway"
```

#### 1.4. Создайте репозиторий на GitHub

1. Откройте https://github.com/new
2. Введите название: `bajar-marketplace`
3. Выберите: **Public** или **Private**
4. НЕ добавляйте README, .gitignore, license (у нас уже есть)
5. Нажмите **Create repository**

#### 1.5. Загрузите код на GitHub

```bash
# Замените YOUR-USERNAME на ваш username GitHub
git remote add origin https://github.com/YOUR-USERNAME/bajar-marketplace.git
git branch -M main
git push -u origin main
```

---

### Шаг 2: Регистрация на Railway

#### 2.1. Перейдите на Railway

Откройте: https://railway.app/

#### 2.2. Войдите через GitHub

1. Нажмите **Login**
2. Выберите **Login with GitHub**
3. Авторизуйте Railway

✅ **Готово!** Вы в панели управления Railway.

---

### Шаг 3: Создание проекта

#### 3.1. Создайте новый проект

1. Нажмите **New Project**
2. Выберите **Deploy from GitHub repo**

#### 3.2. Подключите репозиторий

1. Найдите `bajar-marketplace` в списке
2. Нажмите на него

#### 3.3. Подтвердите развертывание

Railway автоматически:
- ✅ Обнаружит Node.js проект
- ✅ Установит зависимости (`npm install`)
- ✅ Запустит сервер (`npm start`)

⏳ **Подождите 2-3 минуты** пока Railway развернет проект.

---

### Шаг 4: Настройка переменных окружения

#### 4.1. Откройте настройки

1. Кликните на ваш сервис в Railway
2. Перейдите на вкладку **Variables**

#### 4.2. Добавьте переменные

Нажмите **+ New Variable** и добавьте:

```
NODE_ENV=production
```

**Опционально** (можете оставить значения по умолчанию):
```
PORT=3000
MAX_FILE_SIZE=10485760
MAX_FILES=5
```

#### 4.3. Сохраните

Railway автоматически перезапустит сервис.

---

### Шаг 5: Получение публичного URL

#### 5.1. Создайте публичный домен

1. Перейдите на вкладку **Settings**
2. Найдите раздел **Domains**
3. Нажмите **Generate Domain**

Railway создаст URL вида:
```
https://bajar-marketplace-production.up.railway.app
```

#### 5.2. Скопируйте URL

✅ **Это адрес вашего приложения!**

---

### Шаг 6: Обновление API URL в коде

Теперь нужно обновить ваши клиентские файлы, чтобы они использовали Railway URL.

#### 6.1. Откройте `api-client.js`

Найдите строку:
```javascript
const API_BASE_URL = 'http://localhost:3000/api';
```

Замените на:
```javascript
const API_BASE_URL = 'https://ваш-url.up.railway.app/api';
```

#### 6.2. Сохраните и загрузите изменения

```bash
git add api-client.js
git commit -m "Обновлен API URL для Railway"
git push
```

Railway автоматически обновит приложение! 🎉

---

## ✅ Проверка работоспособности

### Тест 1: Откройте сайт

```
https://ваш-url.up.railway.app
```

Вы должны увидеть главную страницу BAJAR.

### Тест 2: Проверьте API

```
https://ваш-url.up.railway.app/api/items
```

Должен вернуть JSON с товарами.

### Тест 3: Админ панель

```
https://ваш-url.up.railway.app/admin.html
```

Попробуйте добавить товар.

---

## 📊 Мониторинг на Railway

### Просмотр логов

1. Откройте ваш проект в Railway
2. Перейдите на вкладку **Deployments**
3. Кликните на последний деплой
4. Нажмите **View Logs**

### Проверка использования ресурсов

1. Вкладка **Metrics**
2. Смотрите CPU, память, сеть

---

## 🔄 Автоматическое обновление

Railway автоматически обновляет приложение при каждом `git push`:

```bash
# Внесите изменения в код
git add .
git commit -m "Описание изменений"
git push
```

⏳ Railway автоматически:
1. Обнаружит изменения
2. Соберет новую версию
3. Развернет её
4. Переключится на новую версию

---

## ⚠️ Важные ограничения бесплатного плана

### Railway Free Tier:

- **500 часов в месяц** выполнения
- **1 GB RAM** на сервис
- **100 GB** исходящего трафика

### Файловая система:

⚠️ **Локальные файлы удаляются при каждом деплое!**

**Проблема:**
- Загруженные изображения в `uploads/` удалятся

**Решение:**
- Используйте облачное хранилище (см. ниже)

---

## 📦 Настройка облачного хранилища (рекомендуется)

Для production используйте облачное хранилище:

### Вариант 1: Cloudinary (рекомендуется) ⭐

**Почему Cloudinary?**
- ✅ Бесплатный план: 25 GB хранилища
- ✅ Автоматическая оптимизация изображений
- ✅ Простая интеграция

**Как настроить:**

1. Зарегистрируйтесь: https://cloudinary.com/
2. Установите пакет:
   ```bash
   npm install cloudinary multer-storage-cloudinary
   ```
3. Добавьте в `.env` (и в Railway Variables):
   ```
   CLOUDINARY_CLOUD_NAME=ваше-имя
   CLOUDINARY_API_KEY=ваш-ключ
   CLOUDINARY_API_SECRET=ваш-секрет
   ```

### Вариант 2: AWS S3

Инструкции в `DEPLOYMENT.md` → "Постоянное хранилище файлов"

---

## 🗄️ Настройка базы данных (рекомендуется)

JSON файл не подходит для production!

### Добавление PostgreSQL на Railway:

#### Шаг 1: Добавьте базу данных

1. В вашем Railway проекте нажмите **+ New**
2. Выберите **Database** → **PostgreSQL**

#### Шаг 2: Подключите к вашему сервису

Railway автоматически создаст переменную `DATABASE_URL`.

#### Шаг 3: Обновите код

Установите пакет:
```bash
npm install pg
```

Используйте `DATABASE_URL` для подключения к PostgreSQL.

Подробности в `DEPLOYMENT.md` → "База данных для Production"

---

## 🔍 Решение проблем

### Проблема: Сайт не открывается

**Решение:**
1. Проверьте логи в Railway
2. Убедитесь что `PORT` переменная установлена
3. Проверьте что `npm start` работает локально

### Проблема: API не работает

**Решение:**
1. Проверьте `API_BASE_URL` в `api-client.js`
2. Убедитесь что используете правильный Railway URL
3. Проверьте CORS настройки

### Проблема: Изображения не загружаются

**Решение:**
1. Используйте Cloudinary (см. выше)
2. Или используйте другое облачное хранилище

### Проблема: База данных сбрасывается

**Решение:**
1. JSON файл сбрасывается при каждом деплое
2. Используйте PostgreSQL (см. выше)

---

## 💰 Стоимость

### Бесплатный план:
- ✅ 500 часов в месяц
- ✅ Отлично для демо и тестирования
- ⚠️ Приложение может "засыпать" при неактивности

### Платные планы:
- **Hobby Plan**: $5/месяц
  - Неограниченные часы
  - Приложение всегда активно
  - Больше ресурсов

---

## 🎉 Готово!

Ваше приложение BAJAR теперь доступно онлайн!

### Что дальше?

- 📊 Настройте мониторинг
- 🗄️ Подключите PostgreSQL
- 📦 Настройте Cloudinary
- 🔒 Настройте CORS для вашего домена
- 📱 Поделитесь ссылкой с друзьями!

---

## 📞 Полезные ссылки

- Railway Dashboard: https://railway.app/dashboard
- Railway Docs: https://docs.railway.app/
- Railway Discord: https://discord.gg/railway

---

**Вопросы?** Проверьте `DEPLOYMENT.md` для детальных инструкций.

**Удачного развертывания! 🚀**
