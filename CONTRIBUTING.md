# 🤝 Руководство для контрибьюторов

Спасибо за интерес к проекту BAJAR! Мы рады любому вкладу.

## 🚀 Как начать

1. **Fork проекта**
   ```bash
   # Создайте fork через GitHub UI
   ```

2. **Клонируйте ваш fork**
   ```bash
   git clone https://github.com/ваш-username/bajar-marketplace.git
   cd bajar-marketplace
   ```

3. **Установите зависимости**
   ```bash
   npm install
   ```

4. **Создайте .env файл**
   ```bash
   cp .env.example .env
   ```

5. **Запустите проект**
   ```bash
   npm run dev
   ```

## 📝 Процесс разработки

### Создание новой ветки

```bash
git checkout -b feature/название-фичи
# или
git checkout -b fix/название-бага
```

### Коммиты

Используйте понятные сообщения коммитов:

```bash
git commit -m "feat: добавлена фильтрация по цене"
git commit -m "fix: исправлена загрузка изображений"
git commit -m "docs: обновлена документация API"
```

**Префиксы:**
- `feat:` - новая функциональность
- `fix:` - исправление бага
- `docs:` - изменения в документации
- `style:` - форматирование кода
- `refactor:` - рефакторинг
- `test:` - добавление тестов
- `chore:` - обновление зависимостей, конфигурации

### Pull Request

1. **Убедитесь что код работает**
   ```bash
   npm run health-check
   npm start
   ```

2. **Запушьте изменения**
   ```bash
   git push origin feature/название-фичи
   ```

3. **Создайте Pull Request через GitHub**
   - Опишите что изменили
   - Добавьте скриншоты если нужно
   - Упомяните связанные issues

## 🎯 Что можно улучшить

### Высокий приоритет
- [ ] Добавить поддержку PostgreSQL/MongoDB
- [ ] Интеграция с облачным хранилищем (S3, Cloudinary)
- [ ] Добавить поиск по товарам
- [ ] Фильтрация и сортировка
- [ ] Пагинация для большого количества товаров

### Средний приоритет
- [ ] Аутентификация и авторизация
- [ ] Система комментариев/отзывов
- [ ] Email уведомления
- [ ] Экспорт данных (CSV, Excel)
- [ ] Темная тема

### Низкий приоритет
- [ ] Мобильное приложение (React Native)
- [ ] PWA поддержка
- [ ] Многоязычность (i18n)
- [ ] Интеграция с платежными системами

## 🐛 Сообщения об ошибках

При создании issue укажите:

1. **Описание проблемы**
2. **Шаги для воспроизведения**
3. **Ожидаемое поведение**
4. **Фактическое поведение**
5. **Скриншоты** (если применимо)
6. **Окружение:**
   - OS: [например, Windows 11]
   - Browser: [например, Chrome 120]
   - Node.js версия: [например, 18.0.0]

## 📐 Стандарты кода

### JavaScript
- Используйте современный ES6+ синтаксис
- Комментируйте сложную логику
- Избегайте глобальных переменных
- Используйте `const` и `let` вместо `var`

### CSS
- Используйте осмысленные имена классов
- Группируйте связанные стили
- Комментируйте сложные селекторы

### HTML
- Семантическая разметка
- Доступность (ARIA атрибуты)
- Валидный HTML5

## ✅ Checklist перед PR

- [ ] Код работает локально
- [ ] Нет console.log в production коде
- [ ] Обновлена документация (если нужно)
- [ ] Добавлены комментарии к сложной логике
- [ ] Проверена работа на разных браузерах
- [ ] `npm run health-check` проходит успешно

## 🙏 Спасибо!

Ваш вклад делает BAJAR лучше для всех!

## 📞 Связь

- GitHub Issues: для вопросов и багов
- Pull Requests: для предложений кода

---

**Happy Coding! 🎉**
