version: '3.8'

services:
  bajar:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    volumes:
      - ./uploads:/app/uploads
      - ./database.json:/app/database.json
    restart: unless-stopped
