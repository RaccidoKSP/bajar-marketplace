# BAJAR - Indian Resale Marketplace

BAJAR - это интернет-магазин перепродажи товаров с системой синхронизации изображений между всеми устройствами.

## 🌟 Возможности

- ✅ Загрузка и хранение фотографий товаров на сервере
- ✅ Синхронизация товаров между всеми устройствами в любой точке мира
- ✅ Поддержка до 5 фотографий на товар
- ✅ Админ-панель для управления товарами
- ✅ Автоматическое сохранение на сервере
- ✅ REST API для работы с данными

## 📁 Структура проекта

```
bajar-marketplace/
├── server.js              # Серверный API (Node.js + Express)
├── api-client.js          # Клиент для работы с API
├── package.json           # Зависимости Node.js
├── index.html             # Главная страница
├── admin.html             # Админ-панель
├── script.js              # JavaScript для главной страницы
├── admin.js               # JavaScript для админ-панели
├── styles.css             # Стили для главной страницы
├── admin.css              # Стили для админ-панели
├── database.json          # База данных (автоматически обновляется)
├── uploads/               # Папка для загруженных изображений
└── README.md              # Этот файл
```

## 🚀 Быстрый старт

### Локальная разработка

#### Шаг 1: Установка Node.js

Если у вас еще не установлен Node.js, скачайте и установите его с [официального сайта](https://nodejs.org/).

Проверьте установку:
```bash
node --version
npm --version
```

#### Шаг 2: Установка зависимостей

Откройте терминал в папке проекта и выполните:

```bash
npm install
```

#### Шаг 3: Настройка переменных окружения

Создайте файл `.env` из шаблона:

```bash
cp .env.example .env
```

#### Шаг 4: Проверка готовности

```bash
npm run health-check
```

Эта команда установит все необходимые пакеты:
- `express` - веб-сервер
- `multer` - обработка загрузки файлов
- `cors` - разрешение кросс-доменных запросов

#### Шаг 5: Запуск сервера

```bash
npm start
```

Или для разработки с автоперезагрузкой:
```bash
npm run dev
```

Сервер запустится на `http://localhost:3000`

#### Шаг 6: Открытие сайта

Откройте в браузере:
- **Главная страница**: `index.html` (можно открыть напрямую или через `http://localhost:3000/index.html`)
- **Админ-панель**: `admin.html`

## 💻 Использование

### Добавление товара

1. Откройте `index.html`
2. Нажмите кнопку "विज्ञापन पोस्ट करें" (Разместить объявление)
3. Заполните форму и загрузите до 5 фотографий
4. Нажмите "विज्ञापन पोस्ट करें" для сохранения

### Управление товарами (Админ-панель)

1. Откройте `admin.html`
2. Здесь вы можете:
   - Просматривать все товары
   - Редактировать товары
   - Удалять товары
   - Фильтровать по категориям
   - Искать товары

## 🌐 Развертывание в Production

### 🚀 Быстрое развертывание (5 минут)

Для развертывания приложения на хостинге смотрите подробные инструкции:

📖 **[QUICKSTART.md](QUICKSTART.md)** - Пошаговые инструкции для популярных платформ:
- Railway (рекомендуется) ⭐
- Render
- Vercel
- Heroku

📚 **[DEPLOYMENT.md](DEPLOYMENT.md)** - Полная документация по развертыванию:
- VPS (Ubuntu/Debian)
- Docker
- Настройка БД для production
- Безопасность и мониторинг

### Поддерживаемые платформы

| Платформа | Сложность | Бесплатный план | Время настройки |
|-----------|-----------|-----------------|-----------------|
| **Railway** | ⭐ Легко | ✅ Да | 3 мин |
| **Render** | ⭐ Легко | ✅ Да | 4 мин |
| **Vercel** | ⭐⭐ Средне | ✅ Да | 3 мин |
| **Heroku** | ⭐⭐ Средне | ❌ Нет | 5 мин |
| **VPS** | ⭐⭐⭐ Сложно | Зависит | 15-30 мин |

### После развертывания

Обновите `API_BASE_URL` в `api-client.js`:
```javascript
const API_BASE_URL = 'https://ваш-домен.com/api';
```

## 🌐 Синхронизация между устройствами

### Как это работает

1. **Сервер хранит все данные**: Все товары и изображения хранятся на сервере
2. **Автоматическая синхронизация**: При открытии сайта данные загружаются с сервера
3. **Доступ с любого устройства**: Открыв сайт на любом устройстве в вашей сети, вы увидите одни и те же товары

### Локальная сеть vs Интернет

#### Для локальной сети (по умолчанию):
```javascript
const API_BASE_URL = 'http://localhost:3000/api';
```

#### Для доступа из интернета:

**Вариант 1: Облачный хостинг (рекомендуется) ⭐**

Смотрите [QUICKSTART.md](QUICKSTART.md) для пошаговых инструкций.

#### Вариант 2: Домашний сервер с проброской портов

1. Настройте проброс порта 3000 на вашем роутере
2. Используйте сервис динамического DNS (например, No-IP, DuckDNS)
3. Обновите `API_BASE_URL` в `api-client.js`:
```javascript
const API_BASE_URL = 'http://ваш-домен.ddns.net:3000/api';
```

#### Вариант 3: Ngrok (для тестирования)

1. Установите [ngrok](https://ngrok.com/)
2. Запустите сервер: `npm start`
3. В новом терминале: `ngrok http 3000`
4. Используйте предоставленный URL (например, `https://abc123.ngrok.io`)
5. Обновите `API_BASE_URL` в `api-client.js`

## 📡 API Endpoints

### Товары

- `GET /api/items` - Получить все товары
- `GET /api/items/:id` - Получить товар по ID
- `POST /api/items` - Создать новый товар
- `PUT /api/items/:id` - Обновить товар
- `DELETE /api/items/:id` - Удалить товар
- `DELETE /api/items` - Удалить все товары

### Загрузка изображений

- `POST /api/upload` - Загрузить изображения (до 5 файлов)

### Здоровье сервера

- `GET /api/health` - Проверка работы сервера

## 🔧 Конфигурация

Все основные настройки можно изменить через файл `.env`:

```env
PORT=3000                    # Порт сервера
NODE_ENV=production          # Окружение (development/production)
DB_FILE=database.json        # Путь к файлу БД
UPLOAD_DIR=uploads           # Папка для загрузок
MAX_FILE_SIZE=10485760       # Макс. размер файла (10MB)
MAX_FILES=5                  # Макс. количество файлов
```

### Примеры настройки

**Изменение порта:**
```env
PORT=8080
```

**Увеличение лимита файлов:**
```env
MAX_FILE_SIZE=20971520  # 20MB
MAX_FILES=10
```


В файлах `script.js` и `handleImagePreview`:
```javascript
if (files.length > 5) { // Измените на нужное количество
```

## 🛡️ Безопасность

⚠️ **Важно**: Текущая реализация предназначена для локального использования или защищенной сети. Для производственного использования рекомендуется:

1. Добавить аутентификацию (JWT, OAuth)
2. Добавить валидацию на стороне сервера
3. Использовать HTTPS
4. Добавить rate limiting
5. Настроить CORS для конкретных доменов
6. Добавить защиту от SQL-инъекций (при использовании БД)

## 🐛 Решение проблем

### Сервер не запускается

```bash
# Проверьте, не занят ли порт 3000
netstat -ano | findstr :3000

# Завершите процесс или измените порт в server.js
```

### Изображения не загружаются

1. Проверьте, что папка `uploads` существует
2. Проверьте права доступа к папке
3. Проверьте консоль браузера на наличие ошибок

### Данные не синхронизируются

1. Убедитесь, что сервер запущен
2. Проверьте, что `API_BASE_URL` в `api-client.js` указывает на правильный адрес
3. Проверьте консоль браузера и логи сервера

## 📝 Примечания

- Все изображения сохраняются в папке `uploads/`
- База данных хранится в файле `database.json`
- При удалении товара связанные изображения также удаляются

## 🔄 Обновление

Чтобы обновить проект:

1. Остановите сервер (Ctrl+C)
2. Обновите файлы проекта
3. Установите новые зависимости: `npm install`
4. Запустите сервер: `npm start`

## 📞 Поддержка

Если у вас возникли вопросы или проблемы:

1. Проверьте логи сервера в терминале
2. Проверьте консоль браузера (F12)
3. Убедитесь, что все зависимости установлены

## 📄 Лицензия

MIT License - используйте свободно для личных и коммерческих проектов.

---

## 🎯 Готовность к Production

Проверьте готовность проекта перед развертыванием:

```bash
npm run health-check
```

Этот скрипт проверит:
- ✅ Версию Node.js
- ✅ Наличие необходимых файлов
- ✅ Зависимости
- ✅ Конфигурацию безопасности

---

**💡 Совет**: Для production использования рекомендуется:
- 🗄️ Использовать настоящую БД (PostgreSQL, MongoDB) вместо JSON файла
- 📦 Использовать облачное хранилище (S3, Cloudinary) для изображений
- 🔒 Настроить HTTPS и CORS
- 📊 Добавить мониторинг и логирование

Подробнее в [DEPLOYMENT.md](DEPLOYMENT.md)
